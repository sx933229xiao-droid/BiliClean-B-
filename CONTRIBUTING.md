# 参与开发

提交代码前请先运行：

```bash
npm run verify
```

功能修改应遵循以下原则：

1. 先为纯逻辑补充或更新 `test/` 中的回归测试；
2. DOM 适配修改应记录所针对的页面和 B 站结构；
3. 不直接长期维护 `dist/`，应修改 `src/` 或 `public/` 后重新构建；
4. 不扩大清单权限，除非 Issue 中明确说明必要性和隐私影响；
5. 状态结构变化必须增加向前迁移，并测试旧备份导入；
6. 保持命中内容静默隐藏，不向网页插入过滤提示；
7. 发布前在 Chrome 和 Edge 的视频页、首页、搜索页执行实机回归。

修复 `docs/KNOWN_ISSUES.md` 中的问题时，请一个问题对应一个可审查的提交，不要把行为变化混入源码恢复提交。
